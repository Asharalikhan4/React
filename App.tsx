import Router from "./src/config/Router";
import { AppProvider } from "./src/provider/AppProvider";

const App = () => {
  return (
    // <AppProvider>
      <Router />
    // </AppProvider>
  );
};

export default App;
